# TalkyPump

TalkyPump firmware project.

The legacy v1 firmware is preserved under `archive/` as a behavioral reference.

Active same-hardware v2 development happens under `firmware/`.

Target hardware:

- ATmega168
- SIM800L
- existing relay control hardware
- existing relay state input
- existing alarm inputs
- existing mains/zero-cross input
- existing indication LED